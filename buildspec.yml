version: 0.2

phases:
  install:
    commands:
      - npm install
  build:
    commands:
     - cd $CODEBUILD_SRC_DIR
     - npm run build
artifacts:
  files:
      - '**/*'
  base-directory: $CODEBUILD_SRC_DIR/dist

